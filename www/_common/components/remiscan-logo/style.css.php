remiscan-logo {
  aspect-ratio: 2 / 1;
  --width: 5rem;
  width: var(--width);
  height: calc(.5 * var(--width));
}

remiscan-logo .background {
  fill: var(--background-color, transparent);
}

remiscan-logo .text {
  fill: var(--text-color, black);
}